apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: collector
spec:
  rules:
    - host: igiari.local
      http:
        paths:
          - path: /
            backend:
              serviceName: collector
              servicePort: 8080