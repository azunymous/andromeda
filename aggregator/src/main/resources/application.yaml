
spring:
  profiles:
    include:
      - local
server:
  port: 9090
management:
  endpoint:
    prometheus:
      enabled: true
  endpoints:
    web:
      exposure:
        include: info, health, prometheus